shared:
    image: node:6

jobs:
    main:
        steps:
            - install: npm install
            - test: npm test
            - write-file: echo "hello" > $SD_ARTIFACTS_DIR/test
            - curl: curl -X PUT -d 'hello' http://store.screwdriver.cd/builds/1057/hello
